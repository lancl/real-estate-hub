import React from "react";

const Search = () => {
  return (
    <form>
      <input
        className="Search"
        type="text"
        placeholder="Search for a city"
      ></input>
    </form>
  );
};

export default Search;
